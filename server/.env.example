# Server Configuration
PORT=5000

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bible-tracker?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Optional: Frontend API URL (if different from default)
REACT_APP_API_URL=http://localhost:5000/api