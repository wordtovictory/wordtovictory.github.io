import React from 'react';
import Button from "@mui/material/Button";

export default function ControlPanel() {
    return (
        <div>
            {/*<Button variant={"contained"}>Clear All</Button>*/}
            <Button variant={"contained"}>Save/load bible study record</Button>
        </div>
    );
};
